

- Move file scanning to background thread (prevents UI freezes)
- Enable ViewBinding in all activities (faster, safer)
- Implement DataStore for username (modern, reactive)
- Setup Room Database for media library.
- Create Data Models.
- Setup Koin dependency injection.
- Move file scanning to background thread (prevents UI freezes)
- Enable ViewBinding in all activities (faster, safer)
- Implement DataStore for username (modern, reactive)
- Survive screen rotations (server won't stop/restart).
- Remove complex logic from StreamingActivity and HomeActivity.
- Make the app easily testable.

